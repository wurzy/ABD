sudo docker run --user 1000:1000 -v /home/wurzy/Desktop/EA/ABD/data/:/var/lib/postgresql/data -e POSTGRES_DB=abd -e POSTGRES_USER=abduser -e POSTGRES_PASSWORD=12345 -p 5432:5432 -it -l pgsql postgres:12

depois fazer docker ps, tirar o nome do executavel.

usar docker exec -it $NOME comandos, para executar comandos


usar noutro terminal: 
docker exec -it inspiring_euler psql -U abduser abd

para entrar na base de dados. Inspiring_euler substituir pelo nome do container, docker ps -a.

para eliminar os ficheiros, docker rm $NOME ou docker rm $(docker ps -a -q)

eliminar tambem os ficheiros da pasta /data/
